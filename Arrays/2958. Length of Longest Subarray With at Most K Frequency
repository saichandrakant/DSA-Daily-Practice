https://leetcode.com/problems/length-of-longest-subarray-with-at-most-k-frequency/submissions/2105800133/?envType=daily-question&envId=2026-08-13
class Solution {
    public int maxSubarrayLength(int[] nums, int k) {
        int l=0;
        int r=0;
        int ans=0;
        HashMap<Integer,Integer> map=new HashMap<>();
        while(r<nums.length)
        {
            map.put(nums[r], map.getOrDefault(nums[r], 0) + 1);
            while(map.get(nums[r])>k)
            {
                map.put(nums[l],map.get(nums[l])-1);
                l++;
            }
                ans=Math.max(ans,r-l+1);
                
                r++;
        }
        return ans;
    }
}
